using UnityEngine;
using System.Collections;

public class GroundObject : MonoBehaviour {
	//This exists only to be put on the groundObject prefabs, so that the PlayerObject
	//script can identify them

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
